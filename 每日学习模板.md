# MarkDone

## 2026-09-17

### 学习内容
- 对 Agent 建立宏观的认识，包含哪些模块，运用什么技术，是如何一步步发展到今天的？
- CLI是什么？

### 学习目标
- [ ] 用自己的话解释什么是 Agent
- [ ] 画出 Agent 的基本工作流程
- [ ] 说清楚 LLM、RAG、工具调用和记忆分别解决什么问题
- [ ] 区分聊天机器人、RAG 系统和 Agent


### 今日收获
#### Agent是什么
以大语言模型为核心，能够理解任务、制定步骤、调用工具、观察结果，并不断调整行动，最终完成目标的软件系统。
普通聊天机器人通常是：
用户提问 → 模型回答
Agent 更接近：
用户提出目标
      ↓
Agent 理解任务并制定计划
      ↓
调用搜索、数据库、代码或业务 API
      ↓
读取工具返回结果
      ↓
继续判断是否需要下一步行动
      ↓
输出最终结果
#### Agent的主要模块
| 模块          | 作用            | 常见技术                          |
| ----------- | ------------- | ----------------------------- |
| 大语言模型       | 理解问题、推理和决策    | GPT、Claude、Qwen 等             |
| Prompt 与上下文 | 告诉模型身份、任务和限制  | System Prompt、结构化输出           |
| 规划模块        | 将复杂任务拆分为多个步骤  | ReAct、任务分解、工作流                |
| 工具调用        | 执行搜索、查询、计算和操作 | Function Calling、MCP、REST API |
| 知识检索        | 从外部知识库获取信息    | RAG、Embedding、向量数据库           |
| 记忆模块        | 保存对话状态和长期信息   | Redis、PostgreSQL、向量库          |
| 状态管理        | 记录当前任务进行到哪一步  | LangGraph、状态机                 |
| 评估与监控       | 检查回答质量和运行过程   | 日志、Tracing、准确率评估              |

#### Agent使用的技术栈
以后你会接触到几类技术：
Python：主要开发语言。
大语言模型 API：提供理解、推理和生成能力。
RAG：让 Agent 能够查询自己的知识库，减少模型胡编。
Function Calling 或 MCP：让模型能够调用外部工具。
工作流框架：管理多个步骤和状态，例如 LangGraph。
数据库和缓存：保存用户信息、任务状态和历史记录。
日志与评估：分析 Agent 是否调用了正确工具、是否完成任务。
#### Agent的发展过程
可以按照下面的脉络理解：
规则系统：依靠人工编写大量 if-else 规则。
机器学习系统：通过数据学习分类、预测和识别能力。
深度学习与 Transformer：模型开始具备更强的语言理解能力。
大语言模型：能够进行开放式对话、总结和推理。
LLM + RAG：模型可以查询外部知识库。
LLM + 工具调用：模型可以搜索、写代码、访问数据库和调用业务接口。
工作流 Agent：多个步骤、状态和工具被组织成完整任务流程。
多 Agent 系统：多个不同角色的 Agent 协作完成复杂任务。
因此，今天的 Agent 并不是凭空出现的，而是：
大语言模型 + 知识检索 + 工具调用 + 状态管理 + 工作流编排

### 开源
DeerFlow 是字节跳动开源的一个 长流程 Agent 运行框架，官方称为 SuperAgent Harness。它不是一个大语言模型，而是把模型、工具、记忆、子 Agent 和执行环境组合起来，让 Agent 能够完成持续几分钟甚至几小时的复杂任务。官方仓库
简单理解：
|概念|作用|
| - | - |
GPT、DeepSeek、Claude | 提供理解和推理能力的模型
LangChain、LangGraph | 构建 Agent 的开发框架
DeerFlow | 已经集成多种能力的完整 Agent 执行平台
CLI | 用户与 Agent 交互的一种入口

DeerFlow 主要包含：
1. 主 Agent：理解任务并制定计划；
2. 子 Agent：分工完成搜索、分析、写作等任务；
3. Skills：可复用的能力模块，例如研究、生成报告、制作幻灯片；
4. Tools：搜索、代码执行、文件处理等工具；
5. Memory：保存对话、用户偏好和任务信息；
6. Sandbox：隔离的代码和文件执行环境。
- DeerFlow 2.0 已经从原来的 Deep Research 框架扩展为更完整的 SuperAgent 平台，并基于 LangChain 和 LangGraph 构建。

### 明日计划

